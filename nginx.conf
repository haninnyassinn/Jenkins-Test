pid /tmp/nginx.pid;

events{}

http{
    client_body_temp_path /tmp/client_temp;

    server {
    listen 8080;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
}
